# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

{{- if .Values.rbac.create }}
{{- if .Values.rbac.namespaceRestricted }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "chrek.fullname" . }}-agent
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "chrek.labels" . | nindent 4 }}
    app.kubernetes.io/component: checkpoint-agent
rules:
  # Watch and annotate pods in this namespace to drive checkpoint/restore lifecycle
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch", "patch", "update"]
  # Emit operational events on pod/restore lifecycle updates
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch", "update"]
  # Update DynamoCheckpoint state only via status subresource
  - apiGroups: ["nvidia.com"]
    resources: ["dynamocheckpoints"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["nvidia.com"]
    resources: ["dynamocheckpoints/status"]
    verbs: ["patch", "update"]
  # Update DynamoComponentDeployment restore conditions only via status subresource
  - apiGroups: ["nvidia.com"]
    resources: ["dynamocomponentdeployments"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["nvidia.com"]
    resources: ["dynamocomponentdeployments/status"]
    verbs: ["patch", "update"]
{{- else }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "chrek.fullname" . }}-agent
  labels:
    {{- include "chrek.labels" . | nindent 4 }}
    app.kubernetes.io/component: checkpoint-agent
rules:
  # Watch and annotate pods cluster-wide on assigned nodes
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch", "patch", "update"]
  # Emit operational events on pod/restore lifecycle updates
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch", "update"]
  # Update DynamoCheckpoint state only via status subresource
  - apiGroups: ["nvidia.com"]
    resources: ["dynamocheckpoints"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["nvidia.com"]
    resources: ["dynamocheckpoints/status"]
    verbs: ["patch", "update"]
  # Update DynamoComponentDeployment restore conditions only via status subresource
  - apiGroups: ["nvidia.com"]
    resources: ["dynamocomponentdeployments"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["nvidia.com"]
    resources: ["dynamocomponentdeployments/status"]
    verbs: ["patch", "update"]
{{- end }}
{{- end }}
